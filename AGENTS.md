# AGENTS.md

## Scope
- These instructions apply to the entire repository unless a deeper `AGENTS.md` overrides them.

## Project Overview
- This repository is a mostly static frontend for `ifsc.stream`.
- The deployed site lives in `public/`.
- There is no frontend build step in this repo.
- PHP is used for data/SEO maintenance scripts in `bin/`.
- Composer manages PHP dependencies used by those scripts.
- Playwright-based integration tests are included for frontend behavior validation.

## Key Paths
- `public/index.html` is the main site entrypoint.
- `public/css/` contains hand-maintained stylesheets.
- `public/js/` contains hand-maintained browser JavaScript.
- `public/events/` contains season event data JSON files.
- `public/structured-data/` contains generated JSON-LD artifacts.
- `public/sitemap.xml` is generated by `bin/build-sitemap`.
- `tests/integration/` contains Playwright integration tests.
- `Makefile` provides the primary test entrypoints (`test`, `test-local`, `test-docker`).

## Working Rules
- Preserve the current no-build architecture; prefer editing the existing HTML, CSS, and vanilla JavaScript directly.
- Keep changes surgical and consistent with the existing style of the touched file.
- Do not introduce frameworks, bundlers, transpilers, or TypeScript unless the user explicitly asks for them.
- Treat `vendor/` as generated dependency output; do not hand-edit it.
- Avoid large formatting-only diffs in `public/index.html`, CSS, or JS files.
- Maintain compatibility with the existing Bootstrap and jQuery-based frontend patterns already in the repo.
- Keep Playwright tests stable by preserving selectors and modal/filter behavior unless intentionally changing UX.

## Data And Assets
- Event JSON files in `public/events/` are source-like data for the site; update them carefully and keep schema compatibility with existing JS.
- Files under `public/structured-data/` and `public/sitemap.xml` are generated artifacts; regenerate with the corresponding `bin/` scripts when source data changes.
- Respect existing ignore rules for generated outputs such as `vendor/`, `node_modules/`, Playwright reports, and current-year event data artifacts.

## Commands
- Install PHP dependencies with `composer install`.
- Install frontend test dependencies with `npm install --no-package-lock`.
- Run a local static preview with `php -S 127.0.0.1:8000 -t public`.
- Run integration tests (auto-picks Docker if available): `make test`.
- Run integration tests on host machine: `make test-local`.
- Run integration tests in Playwright Docker image: `make test-docker`.
- Build structured data files: `php bin/build-structured-data [events-dir] [output-dir] [site-url]`.
- Build sitemap: `php bin/build-sitemap [events-dir] [output-file] [site-url]`.

## Validation
- Playwright integration tests are available in `tests/integration/`.
- For frontend changes, run `make test` when possible and also do lightweight manual verification by serving `public/` locally.
- For script changes, run only the narrowest relevant script command and verify generated artifacts without touching unrelated outputs.

## Ignore Files
- Keep `.gitignore` aligned with generated outputs and local-only files.
- If a change introduces a new generated artifact or dependency directory, update `.gitignore` in the same task.
