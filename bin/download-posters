#!/usr/bin/php
<?php declare(strict_types=1);

$json = json_decode(file_get_contents($argv[1]));

foreach ($json->events as $event) {
    if ($event->poster) {
        copy($event->poster, "public/img/posters/" . basename($event->poster));
    }
}
